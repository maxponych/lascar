[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
log = "0.4.28"
uefi = { version = "0.36.1", features = [ "alloc", "panic_handler", "logger" ] }
